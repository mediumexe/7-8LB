program zad2;
var s,d,a:string;
var z,x,c,b:integer;
  begin
    writeln('Введите строку');
    readln(s);
    d:=s+',';
    writeln('Итоговая строка');
    for c:=1 to 2 do
    begin
      write(a);
    end;
    write(s);
    writeln();
    writeln('Количество символов в строке: ',Length(s));
  end.